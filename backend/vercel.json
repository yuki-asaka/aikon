{
  "devCommand": "uvicorn app.main:app --host 0.0.0.0 --port 3000",
  "builds": [
    {
      "src": "app/main.py",
      "use": "@vercel/python"
    }
  ],
  "routes": [
    {
      "src": "/(.*)",
      "dest": "app/main.py"
    }
  ],
  "installCommand": "pip install poetry && poetry export --without-hashes --format requirements.txt > requirements.txt && pip install -r requirements.txt"
}
